import { createMuiTheme } from '@material-ui/core';

export const theme = createMuiTheme({
  palette: {
    type: 'dark',
  },
  typography: {
    h4: {
      color: '#fff',
    },
    h5: {
      color: '#fff',
    },
    body1: {
      color: '#fff',
    },
    caption: {
      color: '#fff',
      opacity: 0.7,
    },
  },
});
