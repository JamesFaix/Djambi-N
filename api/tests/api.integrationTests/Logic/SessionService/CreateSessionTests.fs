namespace Djambi.Api.IntegrationTests.Logic.SessionService

open Djambi.Api.IntegrationTests

type CreateSessionTests() =
    inherit TestsBase()

    //Create session should work
    
    //Create session should fail if incorrect password

    //Create session should fail if user already has session

    //Create session should fail with locked message on 6th incorrect password attempt