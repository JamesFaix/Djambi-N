namespace Djambi.Api.IntegrationTests.Logic.PlayerService

open Djambi.Api.IntegrationTests

type RemovePlayerTests() =
    inherit TestsBase()

    //Remove player should work if removing self

    //Remove player should work if removing guest of self

    //Remove player should work if admin

    //Remove player should fail if removing different user

    //Remove player should fail if removing guest of different user

    //Remove player should fail if removing virtual player

    //Remove player should fail if removing player not in lobby

    //Remove player should fail if invalid lobbyId

    //Remove player should fail if invalid playerId

    //Remove player should fail if game already started