namespace Djambi.Api.IntegrationTests.Logic.UserService

open Djambi.Api.IntegrationTests

type CreateUserTests() =
    inherit TestsBase()

    //Create user should work

    //Create user should fail if name conflict

    //Create user should fail if signed in and not admin

    //Create user should work if signed in and admin