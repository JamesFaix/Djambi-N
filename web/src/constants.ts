export default class Constants {
    static readonly minRegionCount = 3;

    static readonly maxRegionCount = 8;
}

export enum InputTypes {
    Text = "text",
    Checkbox = "checkbox",
    Password = "password",
    Number = "number"
}